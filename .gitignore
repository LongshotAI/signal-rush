# Node / npm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment / secrets
.env
.env.*
.envrc
!.env.example

# Logs
*.log
logs/

# Build / dist artifacts
dist/
build/
out/
*.tsbuildinfo

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Coverage / test
coverage/
.nyc_output/
*.lcov

# Local state (never publish)
*.bak
*.bak_*
*.tmp
*.orig
*~
.cache/
.turbo/

# Signal Rush runtime data (never commit — contains player balances, transaction history, PII)
# Economy database files (in economy/ dir for tests, and ~/.signal-rush/ at runtime)
economy/economy.db
economy/economy.db-*

# Player identity and event queue (local-only)
player.json
event-queue.json

# OpenClaw / agent-forge workspace state
.openclaw-state/
sessions/
memory/
.dreams/
*.jsonl
